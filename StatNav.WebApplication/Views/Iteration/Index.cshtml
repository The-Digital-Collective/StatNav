@model IEnumerable<StatNav.WebApplication.Models.ExperimentProgramme>

@{
    ViewBag.Title = "Index";
}

<h2>Iterations</h2>
@Html.Partial("_ExperimentTypeSelect", Model)
<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExperimentProgramme.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequiredDurationForSignificance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IterationNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDateTime)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExperimentProgramme.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequiredDurationForSignificance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IterationNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDateTime)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>*@
