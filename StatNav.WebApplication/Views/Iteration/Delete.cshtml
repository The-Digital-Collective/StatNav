@model StatNav.WebApplication.Models.ExperimentIteration

@{
    ViewBag.Title = "Delete";
}

<h4>Delete Iteration</h4>
<div class="alert alert-danger">Are you sure you want to remove this iteration? Removing this iteration will also remove all its candidates</div>
@Html.Partial("_detail", Model)
@using (Html.BeginForm())
{

    <input type="submit" value="Delete" class="btn btn-danger" />
    @Html.ActionLink("Back to List", "Index")
}

